// @ts-ignore
import Ganache = require("ganache-core");

describe("a set of tests using Ganache", () => {
    it("should complete successfully", async () => {
        Ganache.provider()
    });
});
